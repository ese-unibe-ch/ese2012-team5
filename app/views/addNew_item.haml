%link(rel="stylesheet" type="text/css" href="/style.css")

%h1 Register a new item to sell

- if info != nil
   %h2 #{info}

%form( method="POST" )
   Name
   %input(type="text" name="name" )
   %p
   Price
   %input(type="text" name="price")
   %p
   %input(type="submit" value="Submit")

%a{:href => "/"} Discard